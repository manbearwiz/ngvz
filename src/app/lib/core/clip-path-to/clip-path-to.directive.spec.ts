import { ClipPathToDirective } from './clip-path-to.directive';

describe('ClipPathToDirective', () => {
  it('should create an instance', () => {
    const directive = new ClipPathToDirective();
    expect(directive).toBeTruthy();
  });
});
