import { FillOrdinalDirective } from './fill-ordinal.directive';

describe('FillOrdinalDirective', () => {
  it('should create an instance', () => {
    const directive = new FillOrdinalDirective();
    expect(directive).toBeTruthy();
  });
});
