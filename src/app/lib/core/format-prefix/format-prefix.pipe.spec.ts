import { FormatPrefixPipe } from './format-prefix.pipe';

describe('FormatPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
