import { TextColumnDirective } from './text-column.directive';

describe('TextColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColumnDirective();
    expect(directive).toBeTruthy();
  });
});
